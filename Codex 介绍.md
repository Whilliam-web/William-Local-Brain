# Codex 介绍

> 更新日期：2026-07-06  
> 适用场景：想快速理解 Codex 是什么、能做什么，以及怎样把它用在本地仓库和日常开发流程里。

## 一句话概括

Codex 是 OpenAI 的编程智能体，能够阅读代码、修改文件、运行命令、调试问题、审查变更，并在需要时调用工具或联网获取资料。它不是单纯的聊天助手，更像一个可以进入项目现场、和你一起完成工程任务的协作者。

## Codex 能做什么

Codex 适合处理这些工作：

- 理解陌生代码库，梳理目录结构、关键模块和数据流。
- 实现功能、修复 bug、补测试、重构局部代码。
- 运行命令、查看报错、定位失败原因，并迭代修复。
- 做代码审查，指出潜在回归、风险和缺失测试。
- 根据设计、截图、说明文档或已有代码生成前端页面、脚本、工具和文档。
- 把重复工作沉淀成 Skills、插件、脚本或项目级说明，让后续任务更稳定。

## 常见使用入口

Codex 有多个使用入口，适合不同工作方式：

- **Codex CLI**：在终端里使用，适合本地仓库开发、命令行调试、自动化脚本和快速改动。
- **Codex IDE extension**：在 VS Code、Cursor、Windsurf、JetBrains 等 IDE 里使用，适合边看代码边交给 Codex 修改。
- **Codex app**：桌面端交互体验，适合长任务、规划、审查、多工具协作和更强的可视化工作流。
- **Codex Cloud**：把任务交给云端环境处理，适合并行推进、隔离执行或较长时间的工程任务。

## 它如何理解一个项目

Codex 会结合当前对话、项目文件、工具输出和持久说明来判断下一步。对仓库级协作最重要的是 `AGENTS.md`：

- `AGENTS.md` 可以记录项目约定、测试命令、代码风格、提交要求和注意事项。
- Codex 会按作用域读取相关说明；越靠近当前目录的说明越具体。
- 对于长期使用的仓库，把稳定规则写进 `AGENTS.md`，通常比每次在提示词里重复说明更可靠。

在这个知识库里，如果以后希望 Codex 每次都遵循某些写作或整理习惯，可以考虑新增一个 `AGENTS.md`，例如规定文档命名、中文风格、标签格式和引用方式。

## 定制 Codex 的方式

Codex 的可定制层大致可以这样理解：

| 定制方式        | 适合放什么                               |
| ----------- | ----------------------------------- |
| 当前提示词       | 本次任务的目标、限制和临时偏好                     |
| `AGENTS.md` | 仓库内长期有效的协作规则                        |
| Skills      | 可重复执行的专门工作流，例如周报整理、PR 审查、资料归档       |
| 插件          | 可安装、可分发的一组 Skills、工具、应用或配置          |
| MCP         | 连接外部工具和上下文，例如浏览器、Figma、GitHub、文档服务器 |
| Subagents   | 把复杂任务拆给多个专门智能体并行处理                  |

一个实用顺序是：先把项目规则写进 `AGENTS.md`，再把高频流程做成 Skill；只有当流程需要外部系统时，再接 MCP；当任务天然可以并行时，再使用 subagents。

## 使用 Codex 的好提示词

给 Codex 的任务越接近真实工程目标，它越容易产出有用结果。可以这样写：

```text
请阅读这个仓库，给我新增一篇介绍 Codex 的中文 Markdown 文档，放在仓库根目录。内容要适合 Obsidian 知识库阅读，包含它是什么、能做什么、主要入口、定制方式和使用建议。
```

或者：

```text
请修复登录页在移动端按钮溢出的问题。先定位相关文件，再最小化修改，最后运行现有前端检查并告诉我改了哪里。
```

比较有效的提示通常包括：

- 明确交付物：文件、功能、修复、报告、测试结果。
- 明确范围：只改某个模块，还是可以调整相关调用链。
- 明确验证：运行哪些测试、打开哪个页面、检查哪些行为。
- 明确风格：文档面向谁、代码遵循什么习惯、界面应该是什么气质。

## 适合本地知识库的用法

这个仓库看起来是一个 Obsidian 风格的本地知识库。Codex 可以帮你：

- 新建和整理 Markdown 笔记。
- 把零散材料改写成结构化知识卡片。
- 生成目录、索引、标签和双链。
- 批量检查错别字、格式和断链。
- 把某个主题扩展成学习提纲、复习清单或写作素材库。

如果你打算长期用 Codex 维护这个库，建议逐步建立：

- `AGENTS.md`：写清楚这个知识库的整理规则。
- `模板/`：放固定格式的笔记模板。
- `索引/`：按主题建立入口页。
- `资料/`：存放原始材料或引用来源。

## 注意事项

Codex 很适合做工程协作，但仍然需要边界：

- 对会删除、覆盖或迁移大量文件的操作，最好要求它先列计划再执行。
- 对涉及隐私、密钥、账号、财务和法律责任的内容，要人工复核。
- 对外部资料、最新政策、价格、版本和接口行为，要让 Codex 查官方来源。
- 对重要代码变更，要运行测试、查看 diff，并做一次人工审查。

## 官方资料

- [Codex CLI](https://developers.openai.com/codex/cli)
- [Codex IDE extension](https://developers.openai.com/codex/ide)
- [Custom instructions with AGENTS.md](https://developers.openai.com/codex/guides/agents-md)
- [Agent Skills](https://developers.openai.com/codex/skills)
- [Model Context Protocol](https://developers.openai.com/codex/mcp)
- [Customization](https://developers.openai.com/codex/concepts/customization)
