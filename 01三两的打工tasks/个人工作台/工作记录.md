---
title: 个人工作台工作记录
---

# 个人工作台工作记录

## 2026-08-02｜项目入口建立与初步调研

- 用户确认“个人工作台”为近期拟建项目。
- 已建立项目总览、工作记录和视频调研卡。
- 视频可作为需求启发；具体产品与方法待核实。
- “双轨 Agent”视频已独立归档为智能体学习笔记，不关联本项目。
- 未开始方案、原型、技术实现或部署。

## 2026-08-03｜联网补充：搭建模式与 Obsidian 导航页可行性

- 已补充联网调研，形成 [[个人工作台搭建模式调研]]。
- 初步结论：第一版建议采用 Obsidian Markdown 导航页 / 首页工作台；WorkBuddy 暂不作为底座，只作为后续 AI 执行层候选。
- 已记录 B站教程线索，但多数视频未取得完整字幕，当前仅作为进一步观看清单。
- 未开始搭建、插件安装、账号接入、外部服务部署或代码实现。

## 2026-08-03｜首页视觉与每日回顾机制确认

- 用户确认编辑部式深色三栏首页：左侧为节奏与导航，中间为工作／学习／规划变化流，右侧为待判断、风险和最近内容。
- 用户确认第一版采用“原生优先、渐进增强”的混合模式；当前不安装 Dataview、Tasks 等新插件。
- 用户确认以“晚间收刊”培养每日回顾：每日 2–3 分钟，温和展示连续回顾天数与本周完成率，不使用惩罚性打卡。
- 用户确认每日备忘录作为每日变化流和收刊的主记录；项目工作记录与学习笔记保持事实来源角色。
- 已形成 [[个人工作台首页与每日回顾设计方案]]，待用户审阅后再进入实施计划；本次未改动主题、模板、CSS 或插件配置。

## 2026-08-03｜用户暂停

- 用户要求暂停项目并记录当前进度。
- 暂停节点：第一版设计方案已确认并写入；尚未进入实施计划，未修改 Obsidian 主题、CSS snippet、日历备忘录模板、Bases 视图或插件配置。
- 恢复时：先审阅 [[个人工作台首页与每日回顾设计方案]]，再明确实施文件清单与修改顺序。

## 2026-08-04｜恢复并复核设计方案

- 用户要求恢复个人工作台项目，从正式设计方案开始。
- 已恢复到“方案复核中”；当前 Obsidian 为 1.12.7，Cupertino 主题，原生 Bases 和 Daily Notes 已启用。
- 当前没有 CSS snippet 和 `.base` 文件；日历备忘录仍使用现有模板，尚未执行任何实施改动。
- 正在核对原生 Bases 能力与视觉原型之间的差异，确认后再进入实施计划。

## 2026-08-04｜确认 C 模式并形成实施计划

- 用户选择 C：第一版先实施原生七日节奏卡，使用一周后再评估 Dataview 图表。
- 已确认不回填历史日记；节奏数据从启用当天开始积累。
- 已修正响应式要求：窄屏不隐藏左栏，改为三栏依次纵向堆叠。
- 已形成 [[个人工作台第一版实施计划]]，列明首页、日历模板、原生 Bases、CSS snippet、验证和恢复步骤。
- 当前仅完成设计与计划归档，尚未修改工作台、日记模板、CSS snippet、Bases 或插件配置。

## 2026-08-04｜再次暂停，等待回家后实施

- 用户要求暂停进度，回家后再继续。
- 暂停节点：C 模式已确认，[[个人工作台第一版实施计划]] 已写入，尚未开始执行实施任务。
- 未修改：个人工作台首页、日历备忘录模板、原生 Bases、CSS snippet、主题和插件配置。
- 恢复时：直接从实施计划 Task 1“实施前检查与可恢复快照”开始。

## 2026-08-04｜第一版实施与 Base 空白修正

- 用户恢复个人工作台项目，授权按已确认的 C 模式继续实施。
- 已创建 [[个人工作台首页]]、`视图/个人工作台每日节奏.base` 和 `.obsidian/snippets/whiliam-dashboard.css`；已升级 `日历备忘录模板.md`，并在 `appearance.json` 中启用专用 snippet。
- 第一版仍为原生优先：未安装 Dataview、Tasks 或其他社区插件，未回填或改写历史日记，也未自动创建 2026-08-04 日记。
- 静态验证通过：`appearance.json` 可解析；Base YAML 可解析且包含四个卡片视图；首页包含四个指定视图嵌入和 Daily Notes 入口；窄屏 CSS 使用左、中、右纵向堆叠，不隐藏左栏。
- Obsidian 1.13.4 已将 `.base` 文件识别为原生 Bases 视图，打开过程未在应用日志中出现 Base / YAML 解析错误。
- 首次独立打开 Base 时默认停留在“今日脉搏”，由于当天日记尚未创建而显示 0 个结果。已把“七日节奏”调整为第一视图，使独立打开时优先展示最近记录；“今日脉搏”和“今日变化”仍等待当天日记产生数据。
- 已在 Obsidian 中打开 [[个人工作台首页]]。阅读视图下的最终三栏视觉、600px 窄屏效果和信息密度仍需用户现场确认；没有通过自动化点击创建当天日记。
- 实施前原文件恢复副本：`D:\AI Workspaces\Active\2026-08-03-Obsidian个人工作台视觉设计\work\implementation-backup-2026-08-04`。
- Git：知识库原本存在其他未提交改动，本次未执行 `git add`、`git commit`、`git push`，也未创建 worktree。

## 2026-08-04｜A 版编辑部式首页正式部署

- 用户授权使用 Obsidian 插件，并确认以“能力稳定优先、导航页实际功能可用”为研发方向，从 A 版继续。
- 已把 [[个人工作台首页]] 重做为编辑部式深色高密度三栏：顶部实际导航、本地暮色氛围图、动态日期、左栏今日／七日节奏、中栏项目与工作记录、右栏判断／风险／最近更新。
- 已安装并启用 Homepage 4.4.4 与 Dataview 0.5.68。Homepage 启动时打开目标首页并强制使用阅读视图；DataviewJS 与 inline DataviewJS 明确关闭，页面只使用 DQL/inline DQL。
- 原生 `视图/个人工作台每日节奏.base` 继续承担今日脉搏、七日节奏、今日变化和最近收刊；CSS 将 Bases 的绝对定位大卡片压缩为紧凑网格，七日区不再把左栏拉长到约 1400px。
- 功能验证：Homepage 与 Dataview 均出现在 Obsidian 实际加载插件列表；六个内部导航入口均能打开目标文件；Homepage 命令能返回首页且 workspace mode 为 `preview`；未发现 Dataview 查询错误。
- 响应式验证：宽屏为 230px／自适应中栏／255px 三栏；约 500px 窗口在折叠 Obsidian 文件栏后按左、中、右依次堆叠，三栏均可见。
- 验证截图：`D:\AI Workspaces\Active\2026-08-03-Obsidian个人工作台视觉设计\work\obsidian-editorial-dashboard-final.png`；窄屏截图：同目录 `obsidian-editorial-dashboard-cdp-narrow.png`。
- 第二次实施前恢复副本：`D:\AI Workspaces\Active\2026-08-03-Obsidian个人工作台视觉设计\work\implementation-backup-editorial-v2-2026-08-04`。
- 历史日记未回填、未批量改写；未自动创建 2026-08-04 日记；未执行 `git add`、`git commit`、`git push`，未创建或清理 worktree。

## 2026-08-04｜A 版字体可读性调整

- 用户根据真实使用截图反馈数据区字体偏小，并选择 A：局部提高正文与数据层字号，不改变三栏比例。
- 计算样式基线显示普通正文与列表已经是 16px，主要问题集中在栏目标签、Dataview 与 Bases：栏目标签 9px、Dataview 10.5px、表头 8px、Bases 标签 7px、Bases 内容 9px、工具栏 8px。
- 已将工作台根字号调整为 13.5px、H2 为 23px、栏目与分区标签为 10.5px、Dataview 为 12px、表头为 9.5px、Bases 标签为 8.5px、Bases 内容为 10.5px、工具栏为 9.5px；氛围首屏主标题和三栏宽度保持不变。
- 真实 Obsidian 验证：宽屏仍为 230px／自适应中栏／255px 三栏，三栏均可见，无横向溢出、无 Dataview 错误。
- 调整后截图：`D:\AI Workspaces\Active\2026-08-03-Obsidian个人工作台视觉设计\work\obsidian-dashboard-font-scale-a.png`；字号回归脚本：同目录 `verify-dashboard-font-scale.ps1`。
- 本次只修改 `.obsidian/snippets/whiliam-dashboard.css` 与本工作记录；未修改首页内容、Base 数据、插件配置或历史日记，未执行 Git 提交或推送。
- 用户第二次反馈七日节奏卡片内部仍偏小，并要求左栏稍微加宽。已把桌面左栏从 230px 调整为 250px，中等宽度左栏从 190px 调整为 210px，右栏保持原宽度。
- 七日节奏卡片单独使用更高阅读等级：日期 13.5px、字段标签 10px、字段内容 12px、卡片高度 68px；其他 Base 视图不跟随这一轮继续放大。
- 二次验证显示六张卡片均为 190px × 68px，左中右三栏无横向溢出、Dataview 无错误。局部截图：`D:\AI Workspaces\Active\2026-08-03-Obsidian个人工作台视觉设计\work\obsidian-dashboard-weekly-rhythm-large.png`。

## 2026-08-05｜长期记忆、历史索引与晚间复盘部署

- 用户批准按 A 方案继续，并以能力稳定、首页实际功能可用为研发方向。
- 插件源码在独立 Git 仓库 `D:\Whiliam Projects\whiliam-workbench-memory` 开发，当前分支为 `codex/long-term-memory`；知识库根目录没有创建 worktree。
- 已部署本地插件 `Whiliam Workbench Memory`：监听 Vault 的 create／modify／rename／delete 事件，750ms 折叠重复变化，保留 90 天活动，启动时按文件签名补扫离线变化；首次启用仅建立基线。
- 首页中栏新增 `workbench-memory` 自动记忆区块，展示当前项目、最多 6 条候选、温和空状态和晚间复盘入口；Dataview 项目／工作记录查询改为后缀与标签兼容规则。
- 已从 25 份日记、3 份正式项目工作记录和 [[99系统/项目记忆索引|项目记忆索引]] 生成 [[历史业务记录索引]]，提取 263 条带日期、类别和来源链接的记录；旧文件未改写。
- 晚间复盘要求确认候选并回答“推进／新理解／明日一步”三句话；正文使用唯一标记块幂等更新，属性通过 Obsidian API 写入，只有全部写入成功后才记录完成状态。
- 日历备忘录模板已加入受控复盘标记；平时不自动创建日记，只有用户主动完成复盘且当天日记不存在时，才按 Daily Notes 配置创建。
- 自动检查：TypeScript、66 项单元测试、Obsidian 生产构建和历史索引 CLI 构建均通过；静态验收脚本 11／11 通过。
- 真实启动回归发现 Obsidian 在 `workspace.onLayoutReady` 前会为既有文件发出批量 create 事件；已增加启动门控并补充回归测试。修复后重新建立基线：136 个受控文件签名、0 条事件、0 条候选，没有把旧知识库误报为今日变化。
- 真实事件回归：修改本工作记录后，插件在 3 秒内保存 1 条 `modify` 事件和 1 条默认勾选候选，并同步更新文件签名。
- 真实复盘回归：2026-08-05 日记原本不存在；从首页打开复盘弹窗、确认候选并填写三条摘要后，插件按 Daily Notes 配置创建 [[00收件箱/日历备忘录/2026-08-05|2026-08-05]]，写入 `review_status: done`、变化数量和唯一一对复盘标记。
- 首页随后自动切换为“今日已收束／查看今日复盘”；入口能够打开对应日记。完整关闭并重启 Obsidian 后，事件仍为 2 条、候选仍为 2 条且 ID 完全一致，没有重复补记；文件签名自然增加到 137 个。
- 视觉回归发现 HTML 标记在编辑视图中干扰阅读，已改为 Obsidian 原生 `%%` 隐藏标记并保留旧标记迁移兼容。阅读视图不显示技术标记，单元测试同时覆盖重复保存只保留一个复盘块。
- 插件运行态 `data.json` 包含本机签名和活动，不进入 Git；插件目录使用局部 `.gitignore` 排除该文件，源码加载时可在缺失数据的情况下安全初始化。
- 离线补扫实机测试：在 Obsidian 完全关闭后追加本条记录，再重新启动应用；约 18 秒后，启动签名对比把既有“工作记录”候选从 `modify` 更新为 `reconcile`、变化次数从 2 增至 3，候选匹配数仍为 1，没有创建重复候选。
- 插件禁用降级测试：临时从社区插件列表禁用 `whiliam-workbench-memory` 并重启，首页仍保留今日记录、日记索引、项目、工作记录和历史索引等静态导航；自动记忆区块为空，因此结论是“有限降级可导航”，不是功能完全等价。测试后已恢复插件并重启，3 条候选、137 个文件签名和 1 次已完成复盘均保留。
- 调试收尾：关闭了实机调试期间遗留在右侧的 Electron DevTools；该栏不是工作台组成，也不承担运行功能。
- 布局回归修复：用户在 2026-08-05 实际使用中发现中栏候选文字异常换行、七日卡片过度压缩，以及向下滚动后页面闪回。实机计算样式显示候选按钮被 Obsidian 全局规则固定为 32px，但内部内容需要约 51px；已将候选改为自适应高度并设置 58px 可读下限。
- 滚动根因不是普通 CSS 锚点：原首页把 Bases 绝对定位虚拟卡片强制改成静态网格，滚动 18 个采样步时产生 5916 次 DOM 属性更新，页面高度在 1831—1860—1812—1803px 之间变化，请求滚到 900px 时实际回退到约 797px。
- 已把首页的“今日脉搏／七日节奏／最近收刊”从实时 Base 嵌入替换为等价 Dataview DQL 视图，保留原 `.base` 文件作为独立数据入口，不启用 DataviewJS。七日卡片提高到 82px，并增加到 10px 卡间距；替换后首页可见 Base 数量为 0，滚动至 800px 的 8 个采样点页面高度均为 2046px，DOM 更新为 0，没有再次回退。
- 本轮实机截图：`D:\AI Workspaces\Active\2026-08-03-Obsidian个人工作台视觉设计\work\dashboard-layout-scroll-fix-printwindow.png`。
- 实施前恢复副本：`D:\AI Workspaces\Active\2026-08-03-Obsidian个人工作台视觉设计\work\implementation-backup-memory-v1-2026-08-05`。

## 2026-08-05｜最终可靠性收尾暂停

- 用户要求先把问题沉淀进工作内容，暂停本轮代码修改、部署与提交，等下次更新时继续。
- 当前可继续使用的是知识库内已经部署并通过首轮真实验收的稳定版本；本次开发中的可靠性改动只存在于独立源码仓库 `D:\Whiliam Projects\whiliam-workbench-memory`，尚未复制到 `.obsidian/plugins/whiliam-workbench-memory`，因此不会改变当前 Obsidian 的运行状态。
- 源码仓库当前分支为 `codex/long-term-memory`，已提交基线停在 `a086d8e fix: preserve review insertion boundary`。当前有 11 个已修改但未提交的源码／测试文件：`src/controller.ts`、`src/lifecycle.ts`、`src/main.ts`、`src/review-document.ts`、`src/review-modal.ts`、`src/review.ts`、`tests/controller.test.ts`、`tests/lifecycle.test.ts`、`tests/review-document.test.ts`、`tests/review.test.ts`、`tests/ui-contract.test.ts`。
- 已完成但尚未交付的方向包括：统一串行化刷新、文件事件与复盘写入；复盘正文原子写入；保存失败时保留正式日记并标记状态待补存；启动时发现已移动但尚未映射的已完成复盘；渲染单区块失败不拖垮整个首页。上述改动曾在最近一个检查点通过 15 个测试文件、168 项测试及 TypeScript／构建检查，但此后又修改了 frontmatter 保真逻辑，所以该结果不能代表当前工作树已通过。
- 待解决问题一：复盘属性更新不能通过完整解析再序列化而破坏用户原有 YAML 注释、锚点、别名或 merge key。当前已经开始改为“只替换插件管理字段、保留其他 frontmatter 原文”，但最新补丁尚未补齐回归测试，也尚未验证、提交或部署。
- 待解决问题二：启动修复 `completedReviews` 映射时，不能只判断旧路径字符串是否存在；还必须验证对应文件确实是同一天、`review_status: done` 的严格复盘文档，并把路径大小写／分隔符纠正为实际规范路径。否则普通文件占用旧路径时可能阻止正确恢复。
- 待解决问题三：正式复盘 Markdown 已写入、插件状态后台补存再次失败时，目前仅写控制台日志。下次需要增加有限重试后的明确 Notice，不能向用户承诺“后台补存”却在最终失败时保持静默。
- 下次恢复顺序：先为上述三个问题补失败测试并确认失败原因；再实施最小修复；运行完整 `npm run check` 和差异检查；构建并精确部署 `main.js`、`manifest.json`、`styles.css`；最后在真实 Obsidian 中验证首页自动更新、复盘重开与预填、手动刷新、滚动稳定、重启持久化和复盘块不重复。
- Git 状态：本次暂停前没有提交或推送上述开发中改动；知识库正式记录仅更新本项目的《项目总览》和《工作记录》。独立源码仓库当前没有配置远端，后续只能先做本地提交；知识库分支仍通过既有远端保存稳定部署记录。
- 恢复入口：先阅读本节，再从 `D:\Whiliam Projects\whiliam-workbench-memory` 的 `codex/long-term-memory` 分支和未提交工作树继续；不要重新做视觉方案，也不要直接覆盖当前稳定插件。

## 2026-08-09｜最终可靠性收尾完成

- 按用户要求从暂停点恢复，并以现有实施计划完成技术交付，没有重新设计首页或增加新的插件、外部服务和功能范围。
- frontmatter 保真修复：复盘更新只替换插件管理的八个顶层字段，保留其他 YAML 原文；对不能安全定位且带注释、锚点、别名或 merge key 的复杂写法拒绝破坏性回写。真实 Obsidian 编解码器验证中，注释、行内注释、锚点、别名、merge key、正文和唯一复盘标记对均保持正确，旧受控字段被替换。
- 已完成复盘路径修复：启动扫描不再仅判断旧路径是否存在，而是验证文件确实属于同一天、`review_status: done` 且复盘区块可严格解析；路径大小写与分隔符会纠正为 Vault 的实际规范路径，旧路径被普通文件重新占用时也能恢复真正复盘文件。
- 后台补存失败提示：正式复盘 Markdown 已写入但插件状态首次保存失败时仍保留正式内容并安排后台补存；后台补存最终失败会显示 Notice，不再只写开发者控制台。
- 其他同批可靠性改动已交付：复盘正文原子写入；刷新、文件事件、复盘与延迟保存统一串行；刷新事务失败恢复旧状态；同日复盘可重开并从正文预填；已移动但未映射的完成复盘可启动恢复；单个渲染器失败不阻断其他首页区块；手动刷新使用 single-flight，避免重复执行和重复通知。
- 自动验证：`npm run check` 通过，包含 15 个测试文件、172 项测试、TypeScript 类型检查、生产插件构建和历史索引 CLI 构建；`git diff --check` 通过；长期工作台静态检查 11／11 通过。
- 精确部署：只从独立源码仓库复制 `main.js`、`manifest.json`、`styles.css` 到 `.obsidian/plugins/whiliam-workbench-memory`。三个文件的 SHA-256 与源码构建逐一一致；部署前版本保存在 `D:\AI Workspaces\Active\2026-08-03-Obsidian个人工作台视觉设计\work\implementation-backup-final-2026-08-09`。
- 实机运行：Obsidian 1.13.4 中插件已启用，首页自动记忆区可见；“刷新今日工作记忆”命令存在且执行成功；2026-08-09 自动候选正常出现；可见候选高度为 58px。
- 滚动回归：滚动容器到 800px 后四次采样均保持 `scrollTop=800`、页面高度 2094px，没有闪回。
- 复盘回归：重新打开 2026-08-05 已完成复盘时，三项回答全部从日记正文预填，五行候选中原存一行保持勾选，保存按钮可用且无解析错误；验证后未执行保存，不改写旧复盘。
- 重启回归：完整关闭并重启 Obsidian 前后均为 30 条事件、29 个候选、149 个文件签名和 2 次已完成复盘；2026-08-09 唯一候选 ID 与路径完全一致，没有重复补记；首页恢复可见，右侧 DevTools 未打开。
- 受控临时文件：实机 frontmatter 回归使用的 `99系统/Workbench最终验证临时.md` 已在验证结束时删除；首次使用隐藏文件名产生但未被 Vault 索引的验证文件也已删除，两个路径均确认不存在。
- 视觉证据：`D:\AI Workspaces\Active\2026-08-03-Obsidian个人工作台视觉设计\work\dashboard-final-2026-08-09.png`；结构化验收记录：同任务目录 `work/long-term-memory/runtime-validation.json`。
- 源码 Git：独立仓库 `D:\Whiliam Projects\whiliam-workbench-memory` 的 `codex/long-term-memory` 分支已提交 `4fcda33 fix: complete reliable review lifecycle`，并以 `1314adc chore: normalize plugin manifest` 规范交付清单文件；工作树干净。该仓库没有配置远端，因此没有推送或创建 PR。
- 知识库 Git：插件部署文件与正式项目记录已在 `codex/obsidian-personal-workbench` 分支提交为 `08f8500 feat: complete personal workbench reliability`。向 `origin/codex/obsidian-personal-workbench` 推送时，GitHub `443` 端口连接失败；本地提交完整保留，网络恢复后可执行 `git push origin codex/obsidian-personal-workbench` 续传。本次没有把知识库内其他未提交改动纳入个人工作台提交。
- 项目结论：个人工作台的每日自动更新、晚间复盘、历史记忆、重启恢复和安全降级链路已经完成技术交付。后续一周只观察真实候选质量与习惯形成效果；如需调整信息密度或过滤规则，应基于这段真实使用记录另开迭代，不回滚本次稳定性基线。

## 2026-08-06｜8.6 会议纪要整理与迭代路演日方案

- 用户提供 8.6 Delta Space 会议完整转写稿，已整理更新 `会议纪要/8.6日会议纪要.md`（从简短版扩写为完整版）：王坛参访（团建+柯桥区/王坛镇政企交流、三方露出）、宣传线排期（8.10 空间官宣、8.20 启动月官宣、9.15 启动日提前 7 天）、线下物料与装修排期、启动月活动体系（四周结构+活动分类）、项目来源线（8 月底项目招募、株洲、湖南黑客松、华为）、SOP 能力沉淀、官网与视觉、15 项行动项。
- 与用户逐步讨论确定"项目来源活动模式"：目标品牌+项目+合作方三合一、受众浙大学生/校友团队、形式路演大赛、8 月底首场、完整漏斗（报名入库→初筛→路演→评委表态→48h 跟进）、混合层级评委、定制激励、小而精规模、验证型成功标准。
- 已产出交付方案 `Delta Space/Delt Space 启动月活动/迭代路演日策划方案 V0.1.md`，供用户与余奕对齐（含意向书口径、评委确认等待办）。
- 用户暂不需要岳麓之夜/湖南黑客松独立方案文档，相关信息已保留在 8.6 会议纪要的行动项与待确认中。
